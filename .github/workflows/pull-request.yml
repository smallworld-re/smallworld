name: Pull Request
on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopen, edited]

jobs:
  podman-build:
    runs-on: smallworld-arc
    container: 
      image: quay.io/podman/stable:v5
    steps:
      - name: build
        run: |
          sleep 10000000
          podman run --rm -it docker.io/hello-world:latest