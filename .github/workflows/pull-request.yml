name: Pull Request

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopen, edited]

jobs:
  Test:
    runs-on: ubuntu-latest
    container:
      image: aflplusplus/aflplusplus:latest
    steps:
      - uses: actions/checkout@v3
      - name: Check Title
        env:
          TITLE: ${{ github.event.pull_request.title }}
        run: |
          bash .github/workflows/scripts/conventional.sh "$TITLE"
      - name: Dependencies
        run: |
          apt-get update -y
          apt-get install -y nasm
      - name: Install
        run: |
          python3 -m pip install -e .[development] -c constraints.txt
          which afl-showmap
          which python
          which python3
          export 
          PYTHONPATH=/root/.local/lib/python3.10/site-packages:$PYTHONPATH python3 -c "import sys; print(sys.path)"
          PYTHONPATH=/root/.local/lib/python3.10/site-packages:$PYTHONPATH python -c "import sys; print(sys.path)"
          PYTHONPATH=/root/.local/lib/python3.10/site-packages:$PYTHONPATH python3 -m pip list
          PYTHONPATH=/root/.local/lib/python3.10/site-packages:$PYTHONPATH python3 -c "import smallworld; print(smallworld)"
          PYTHONPATH=/root/.local/lib/python3.10/site-packages:$PYTHONPATH python3 -c "import unicornafl; print(unicornafl)"
          PYTHONPATH=/root/.local/lib/python3.10/site-packages:$PYTHONPATH python -c "import smallworld; print(smallworld)"
          PYTHONPATH=/root/.local/lib/python3.10/site-packages:$PYTHONPATH python -c "import unicornafl; print(unicornafl)"
          cd tests && ls
          PYTHONPATH=/root/.local/lib/python3.10/site-packages:$PYTHONPATH AFL_DEBUG=1 afl-showmap -U -m none -o /dev/stdout -- python3 afl_fuzz.py fuzz_inputs/good_input
      - name: Build
        run: |
          make -C tests
      - name: Test
        run: |
          which afl-showmap
          which python
          which python3
          PYTHONPATH=/root/.local/lib/python3.10/site-packages:$PYTHONPATH python3 -c "import smallworld; print(smallworld)"
          PYTHONPATH=/root/.local/lib/python3.10/site-packages:$PYTHONPATH python3 -c "import unicornafl; print(unicornafl)"
          PYTHONPATH=/root/.local/lib/python3.10/site-packages:$PYTHONPATH python -c "import smallworld; print(smallworld)"
          PYTHONPATH=/root/.local/lib/python3.10/site-packages:$PYTHONPATH python -c "import unicornafl; print(unicornafl)"
          cd tests && ls
          cd tests && PYTHONPATH=/root/.local/lib/python3.10/site-packages:$PYTHONPATH AFL_DEBUG=1 afl-showmap -U -m none -o /dev/stdout -- python3 afl_fuzz.py fuzz_inputs/good_input
          cd tests && PYTHONPATH=/root/.local/lib/python3.10/site-packages:$PYTHONPATH python3 unit.py
          cd tests && PYTHONPATH=/root/.local/lib/python3.10/site-packages:$PYTHONPATH python3 integration.py
